export function Footer() {
  return (
    <footer className="sources">
      Sources: NOAA CO-OPS tide predictions · NWS api.weather.gov forecasts · NDBC nearshore buoy · Open-Meteo Marine
      &amp; hourly forecast models · solunar &amp; astronomy computed locally · local reports curated manually.
      <br />
      Data refreshes on a schedule (see the update-conditions workflow) rather than on every page load.
    </footer>
  );
}
